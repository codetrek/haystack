FROM golang:1.23

WORKDIR /app

# Copy dependency files first for better caching
COPY go.mod go.sum ./
RUN go mod download

# Copy everything
COPY . .

CMD ["bash", "-c", "ulimit -u 256 && go test ./... -count=1 -timeout 5m"]
